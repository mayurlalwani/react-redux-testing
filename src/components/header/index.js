import React from "react";
import "./styles.scss";

const Header = (props) => {
  return (
    <header>
      <div className="wrap">
        <div className="logo">
          Logo
          {/* <img src={Logo} alt="logo" /> */}
        </div>
      </div>
    </header>
  );
};

export default Header;
